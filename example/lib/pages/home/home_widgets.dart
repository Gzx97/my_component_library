import 'package:flutter/material.dart';

class HomeCustomWidget extends StatelessWidget {
  const HomeCustomWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // 具体组件样式和内容，可根据需求定制
      child: Text('Home Custom Widget'),
    );
  }
}
